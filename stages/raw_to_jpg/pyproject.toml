[project]
name = "agir-stage-raw-to-jpg"
version = "1.5.0"  # Stage version (independent)
dependencies = [
    # Stage-local image processing dependencies.
    "rawpy>=0.18.0",
    "pillow>=10.0.0",
]
