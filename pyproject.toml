[project]
name = "agir-db"
version = "0.1.0"
description = "Shared Postgres tools for AgIR"
requires-python = ">=3.12"
dependencies = [
  "psycopg2-binary",
  "pyyaml",
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
